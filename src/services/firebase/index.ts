// Arquivo para exportar todos os serviços do Firebase
export * from './config';
export * from './auth';import { auth } from './config';

// Aqui você pode colocar helpers, ex:
// export async function login(email: string, senha: string) { ... }

export { auth };

